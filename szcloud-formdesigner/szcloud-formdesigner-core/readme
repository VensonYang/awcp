模块设计
	design-(parse)->DB/file-parse->html
			  		 |-parse->pdf
design
	核心类：动态页面、页面组件、页面动作等
	主要是对页面数据、页面组件、页面动作、页面导出等信息的配置（增删改查）
		页面数据设计：数据来源有两种，一种是根据元数据来选择设计，另外一种是自定义。
			元数据选择：设置别名，勾选元数据和对应的元数据属性
			自定义：数据名称、自定义sql语句，提供内置参数的占位符
		页面组件设计：
			选择组件类型新增 or 修改已经设置的组件类型（组件类型不通，设置页面不一样）
			组件设计涉及基本信息、布局、校验等
		页面动作设计：
			动作类型不一样，脚本代码不一样，确定动作类型后，自动填充部分脚本
		
	输入：根据系统提供的接口进行配置
	输出：对应数据库表中的记录
runtime			  	
	核心类：文档
	主要是将设计的页面和用户提交的数据进行关联
	输入：pageId，流程id，用户填写表单数据等
	输出：根据格式进行输出为html、pdf等
parse
	核心类：页面组件转化类、页面动作转化类、页面转化类等
	主要是将设计的代码根据需要转化成目标格式
	输入：模版+数据
	输出：指定格式的文档，如html、pdf文件等
macro
	提供文件、邮件、短信、日期、session、数据库、用户、角色、权限、组织、流程操作等相关的工具
	
需要做的页面(共8类)
	页面列表（详细信息列表、缩略图列表等），搜索
	新增or修改页面信息的页面
	模型选择器页面
	元数据配置页面
	新增or修改组件页面（组件属性设置页面）[根据组件类型进行开发]   主要工作量
	动作列表页面
	新增or修改动作页面（动作属性设置）
	页面布局设置页面